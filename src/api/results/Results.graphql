type Result {
  _id: ID!
  score: Int!
}

input ResultInput {
  _id: ID
  score: Int!
}

type Mutation {
  addResult(result: ResultInput): Result
  removeResult(ID: String): String
}

type Query {
  results: [Result]
}
