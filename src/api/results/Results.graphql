type Result {
  _id: ID!
  score: Int!
}

input ResultInput {
  _id: ID
  score: Int!
}

type Mutation {
  addResult(result: ResultInput): Result
  removeResult(_id: ID!): Result
}

type Query {
  results: [Result]
}
